# Good Inside Demo Project

I used flavors, so run accordingly: 

#Prod
flutter run -d {simulator or emulator} --flavor prod lib/main_prod.dart

#Dev
flutter run -d {simulator or emulator} --flavor dev